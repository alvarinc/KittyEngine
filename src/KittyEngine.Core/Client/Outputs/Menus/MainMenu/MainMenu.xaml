<UserControl x:Class="KittyEngine.Core.Client.Outputs.Menus.OutGameMenu.MainMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KittyEngine.Core.Client.Outputs.Menus.OutGameMenu"
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition />
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>

        <Canvas Grid.Column="3" Grid.Row="0">
            <Button Canvas.Top="15" Canvas.Right="10" x:Name="btnExit" ToolTip="Exit" Background="Transparent" Click="btnExit_Click"  >
                <Image Height="40" Width="40"  Source="pack://application:,,,/KihonEngine;component/Content/Images/Icons/icon-exit-transparent.png" Stretch="Uniform" />
            </Button>
        </Canvas>

        <Grid Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="100"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>
            <Border Grid.RowSpan="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Border CornerRadius="10, 10, 10, 10" BorderBrush="DimGray" BorderThickness="1, 1, 1, 1" />
            </Border>
            
            <TextBlock Grid.Row="0" Grid.Column="1" Text="Join game" FontSize="24" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Arial"/>
            <Button Grid.Row="2" Width="200" Height="50" x:Name="btnStart" Content="Join" Click="btnStart_Click"/>
        </Grid>
    </Grid>
</UserControl>
